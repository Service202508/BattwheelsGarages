name: Security Audit

on:
  pull_request:
    branches: [main, master, develop]
  push:
    branches: [main, master]

jobs:
  pip-audit:
    name: Backend — pip-audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: backend
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install pip-audit

      - name: Run pip-audit (fail on any unfixed vulnerability)
        working-directory: backend
        run: |
          # Ignores ecdsa CVE-2024-23342 (GHSA-wj6h-64fc-37mp) — no upstream fix available.
          # All other HIGH/CRITICAL findings will fail this job.
          pip-audit \
            --format=columns \
            --ignore-vuln GHSA-wj6h-64fc-37mp

  npm-audit:
    name: Frontend — npm audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        working-directory: frontend
        run: npm ci --prefer-offline

      - name: Run npm audit (fail on HIGH/CRITICAL)
        working-directory: frontend
        run: npm audit --audit-level=high
