# Battwheels OS - Backend Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit actual .env files to version control

# ===========================================
# DATABASE (Required)
# ===========================================
MONGO_URL=mongodb://localhost:27017
DB_NAME=battwheels_db

# ===========================================
# SECURITY (Required)
# ===========================================
# Generate a strong secret: openssl rand -hex 32
JWT_SECRET=your-secure-jwt-secret-here-min-32-chars

# ===========================================
# CORS (Required)
# ===========================================
# In production, set to your exact frontend URL
CORS_ORIGINS=https://your-production-domain.com

# ===========================================
# APP METADATA (Required in production)
# ===========================================
APP_URL=https://your-production-domain.com
ENVIRONMENT=production
APP_VERSION=1.0.0

# ===========================================
# AI/LLM Integration (Optional)
# Get key from Emergent platform → Profile → Universal Key
# ===========================================
EMERGENT_LLM_KEY=sk-emergent-your-key-here

# ===========================================
# SENTRY ERROR MONITORING (Highly Recommended)
# 1. Go to https://sentry.io → New Project → FastAPI
# 2. Copy the DSN from project settings
# Without this you are blind to production errors
# ===========================================
SENTRY_DSN=https://xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx@oNNNNN.ingest.sentry.io/NNNNNNN

# ===========================================
# EMAIL SERVICE - Resend (Critical)
# Required for: invoices, tickets, SLA alerts, invites
# 1. Go to https://resend.com → API Keys → Create key
# 2. Set SENDER_EMAIL to a verified domain address
# Without this, all transactional emails silently fail
# ===========================================
RESEND_API_KEY=re_REDACTED
SENDER_EMAIL=noreply@yourdomain.com

# ===========================================
# PAYMENT GATEWAY - Razorpay (Critical)
# Required for: online payment links, refunds
# TEST keys: https://dashboard.razorpay.com (test mode toggle)
# LIVE keys: switch Test Mode OFF → Settings → API Keys
# Webhook URL: https://your-domain.com/api/payments/razorpay/webhook
# ===========================================
RAZORPAY_KEY_ID=rzp_live_REDACTED
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
RAZORPAY_WEBHOOK_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===========================================
# PAYMENT GATEWAY - Stripe (Optional)
# Get credentials from https://dashboard.stripe.com
# ===========================================
STRIPE_API_KEY=sk_live_REDACTED
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===========================================
# ZOHO BOOKS INTEGRATION (Optional)
# Get credentials from https://api-console.zoho.in
# ===========================================
ZOHO_CLIENT_ID=1000.your_client_id
ZOHO_CLIENT_SECRET=your_client_secret
ZOHO_REFRESH_TOKEN=1000.your_refresh_token
ZOHO_API_BASE_URL=https://www.zohoapis.in
ZOHO_ACCOUNTS_URL=https://accounts.zoho.in
ZOHO_ORGANIZATION_ID=your_organization_id

# ===========================================
# E-INVOICE IRP (Configure per-org via Settings UI)
# NOTE: IRP credentials are stored per-organization in the database,
# NOT in this file. Go to Settings → Finance → E-Invoice to configure.
# The env vars below are LEGACY fallbacks — prefer org settings UI.
# Get credentials from: https://einvoice1.gst.gov.in
# Switch SANDBOX → PRODUCTION in org settings after testing.
# ===========================================
# EINVOICE_CLIENT_ID=your_irp_client_id
# EINVOICE_CLIENT_SECRET=your_irp_secret
# EINVOICE_USERNAME=your_irp_username
# EINVOICE_PASSWORD=your_irp_password
# EINVOICE_GSTIN=your_gstin
